<%= form_for [@course, @cohort] do |f| %>
 New Cohort for <%=@course.name%>
     </div>

    <div>
        <%= f.label :start %>
        <%= f.date_field :start%>
    </div>

    <div>
        <%= f.label :end %>
        <%= f.date_field :end %>
        
    </div>
    
     <div>
        <select name="instructorSelect">
             <% Instructor.all.each do |instructor| %>
                 <option value="<%= instructor.id %>"><%= instructor.l_name %></option>
             <% end %>

            </select>
    </div>
 
  
        <%= f.hidden_field :course_id, value: params[:course_id] %>
        <%= f.hidden_field :name, value: @course.name%>
    

        
    <div>   
        <%= f.submit %>
    </div>

<% end %>